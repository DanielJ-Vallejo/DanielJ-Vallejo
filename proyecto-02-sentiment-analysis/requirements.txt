# Núcleo: preparación de datos + tests (ligero, sin GPU).
# Las dependencias de entrenamiento (torch, transformers, ...) son opcionales y
# viven en requirements-train.txt — solo se necesitan para reentrenar el modelo.
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
openpyxl>=3.1
pytest>=8.0
